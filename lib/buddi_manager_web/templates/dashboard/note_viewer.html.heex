<div style="height:88vh; overflow-y:auto;">
  <div class="d-flex justify-content-start flex-wrap mt-3 mx-5" style="gap:0.5rem;">
    <%= for note <- @notes do %>
      <.note_card
        note={note}
        note_delete_path={Routes.note_path(@conn, :delete, note)}
        note_show_path={Routes.note_path(@conn, :show, note)}
      />
    <% end %>
  </div>
</div>
