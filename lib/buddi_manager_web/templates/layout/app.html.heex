<!DOCTYPE html>
<!--This layout is used as a default layout-->
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta content="buddi" name="author" />

    <meta name="csrf-token" content={csrf_token_value()} />
    <%= live_title_tag(assigns[:page_title] || "Buddi Manager") %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <link phx-track-static rel="stylesheet" href={Routes.static_path(@conn, "/assets/app.css")} />
    <script
      defer
      phx-track-static
      type="text/javascript"
      src={Routes.static_path(@conn, "/assets/app.js")}
    >
    </script>
  </head>

  <body class="root container" style="height: 100%; background-color: rgb(245, 242, 240);">
    <.top_bar current_user={@current_user} />

    <div class="d-flex">
      <%= if assigns.current_user do %>
        <div class="mx-2" style="width:10%">
          <.sidebar />
        </div>
        <div style="width:100%; height:90vh; overflow-y:auto;">
          <%= @inner_content %>
        </div>
      <% else %>
        <%= @inner_content %>
      <% end %>

      <.footer />
    </div>
  </body>
</html>
